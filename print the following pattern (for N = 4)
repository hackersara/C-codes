Take N (number of rows), print the following pattern (for N = 4)



#include <iostream>
using namespace std;
int main() 
{ 
    int N;
    cin>>N;
    int rowno=1;
    int number=1;
    while(rowno<=N)
    {
      int i=1;
    while(i<=rowno)
    {
        cout<<number<<" ";
        i++;
        number++;
    }
    cout<<endl;
    rowno=rowno+1;
    }
   
    return 0;
}
